# AI 활용 개발 타임라인

> 공모전 제출용 작업 기록입니다. 각 작업일에 AI가 맡은 역할, 사람이 검토·결정한 내용, 실제 반영 결과를 함께 남깁니다. AI의 제안은 모두 개발자가 검토한 뒤 적용합니다.

## 기록 원칙

- 날짜별로 작업 목적과 산출물을 분리해 기록한다.
- AI가 한 일(분석·아이디어·코드 초안·디버깅·문서화)과 개발자가 한 최종 판단을 구분한다.
- 프롬프트 전문 대신 목적과 핵심 요청을 요약한다. 필요 시 대화 링크나 캡처를 별도 첨부한다.
- 테스트 결과와 미반영 사유도 남겨, AI 결과를 무비판적으로 사용하지 않았음을 명확히 한다.

---

## 2026-07-22 — 프로젝트 구조 분석 및 개발 기록 체계 수립

### 작업 목표

- 기존 Unity 프로젝트의 실행 흐름, 데이터 구조, 핵심 게임 루프를 파악한다.
- 이후 개발 과정에서 AI 활용 내역을 일관되게 남길 수 있는 기록 형식을 만든다.

### AI 활용 방식

- 프로젝트 폴더, 씬, C# 스크립트, ScriptableObject 자산, PRD를 분석해 시스템 간 연결 관계를 정리했다.
- CCTV 관찰, 이상현상 생성·처리, 보고 UI, 일차 진행을 중심으로 코드 책임을 분류했다.
- 공모전 증빙에 적합하도록 AI의 역할과 개발자의 검토·결정이 함께 남는 타임라인 문서 양식을 설계했다.

### 개발자 검토 및 결정

- AI 분석 결과를 바탕으로 현재 프로젝트의 MVP 범위를 `CCTV 관찰 → 이상현상 발견 → 3단계 보고 → 성공/실패 판정`으로 확인했다.
- 이후 기능 개발 시 이 문서에 날짜별로 작업 목적, AI 활용, 검토 결과, 테스트 결과를 추가하기로 했다.

### 산출물 및 확인 결과

- 이 문서 생성: `AI_활용_개발_타임라인.md`
- 분석 대상: Unity 6.0.0.48f1, 빌드 씬 `Assets/Scenes/CCTVTest.unity`
- 현재 데이터 기반 콘텐츠: 3개 CCTV 구역, `DayDefinition`, `AnomalyDefinition`, 시각 효과 프로필

---

## 2026-07-22 — 완성형 PRD 기준 반영

### 작업 목표

- `미기록_구역_완성형_PRD_Codex용.md`를 분석해 이후 구현의 제품 기준과 제약을 명확히 한다.

### AI 활용 방식

- PRD의 핵심 게임 루프, 수용 기준, 데이터 모델, 개발 단계, 열린 결정 사항을 요약·대조했다.
- 현재 구현에 적용할 우선순위와 임의로 결정하면 안 되는 항목을 식별했다.

### 개발자 검토 및 결정

- 이후 기능 요청은 PRD의 관련 절과 수용 기준을 먼저 확인한 뒤 구현한다.
- 현장 이동 중 일반 이상현상 타이머 정지, 성공 노이즈의 제한적 사용, 기준 상태 스냅샷 복원, 데이터 기반 콘텐츠 확장을 핵심 제약으로 유지한다.
- 해상도, 오보고 페널티 세부값, 보고 중 시간 처리 등 PRD의 열린 결정 사항은 별도 결정 전까지 임의로 확정하지 않는다.

### 구현 및 테스트 결과

- 코드 변경 없음. 기준 문서 분석 및 개발 판단 규칙 반영 완료.

---

## 2026-07-22 — 핵심 로직·시나리오·이상현상 콘텐츠 기준 통합 분석

### 작업 목표

- 핵심 로직 시나리오 및 기능 테스트, 시나리오 바이블, 이상현상 기획·선행조건 목록을 PRD와 연결한다.
- 이후 구현의 기능 검증, 서사 플래그, 콘텐츠 발생 조건을 명확히 한다.

### AI 활용 방식

- 기능 테스트 통합문서에서 핵심 시나리오 10개와 기능 테스트 20개를 추출해 MVP 검증 항목으로 분류했다.
- 시나리오 바이블에서 7일 서사 구조, 필수 사건, 엔딩 분기, 구현 플래그, 서사 공정성 규칙을 분석했다.
- 이상현상 기획표에서 일차별 콘텐츠 수량, 필수·랜덤·조건부 이벤트, 연쇄 이벤트, 현장 사건의 선행 조건을 구조화했다.

### 개발자 검토 및 결정

- 현재 개발 우선순위는 핵심 시나리오 `SCN-01~09`와 P0 기능 테스트를 충족하는 CCTV MVP 완성으로 둔다.
- 이후 콘텐츠 시스템은 이벤트 ID, 선행 이벤트, 조건, 분류, 발생 행동을 데이터로 관리한다.
- 연쇄 이벤트는 미발견 결과를 다음 단계로 전달하고, 현장 이벤트 중에는 일반 이상현상 생성·타이머를 정지한다.
- 변조된 UI·기준 화면은 서사 연출로 사용할 수 있으나, 실제 보고 입력값과 판정값을 바꾸어 불공정한 오답을 만들지 않는다.
- 7일차의 엔딩 선택은 최종 보고·현장 이동·획득 플래그를 통해 처리하며, 엔딩과 세부 설정의 열린 결정 사항은 별도 확정 전까지 고정하지 않는다.

### 구현 및 테스트 결과

- 코드 변경 없음. 분석 결과를 이후 기능·콘텐츠·QA 작업의 기준으로 반영 완료.
- 확인 대상: `게임_핵심로직_시나리오_기능테스트.xlsm`, `미기록_구역_시나리오_바이블.docx`, `미기록_구역_이상현상_기획목록_선행조건정리.xlsx`

---

## 2026-07-22 — AI 활용 기능 및 공모전 시연 요소 발굴

### 작업 목표

- 프로젝트의 CCTV 관찰·기록 공포 구조에 맞는 AI 활용 기능을 도출한다.
- 구현 가능성과 심사 시연 효과를 함께 고려해 우선순위를 정한다.

### AI 활용 방식

- PRD, 시나리오 바이블, 이상현상 이벤트 구조를 바탕으로 AI가 게임 안에서 맡을 수 있는 역할과 개발 보조 역할을 분리해 아이디어를 발산·정리했다.
- AI 적용이 게임 규칙의 공정성, 1인 개발 범위, 심사 증빙 가능성을 해치지 않는지 검토했다.

### 개발자 검토 및 결정

- 추천 우선순위는 AI 브리핑·평가 문서 개인화, 이상현상 콘텐츠 제작 보조, 플레이 로그 기반 난이도 진단, 음성 기반 M0 시스템 연출 순으로 검토한다.
- AI가 보고 정답이나 판정을 임의로 바꾸지 않도록, 핵심 판정은 기존의 결정적 데이터 규칙으로 유지한다.

### 구현 및 테스트 결과

- 코드 변경 없음. 후보 기능 및 시연 전략을 도출했으며, 다음 기능 요청에서 범위를 확정해 구현한다.

---

## 2026-07-22 — 1일차 핵심 루프 구현 격차 분석

### 작업 목표

- 현재 Unity 씬·데이터·스크립트를 1일차 기획 루프와 대조해 구현 우선순위를 정한다.

### AI 활용 방식

- `TestDayDefinition`, D1 이상현상 자산, CCTV 보고 UI, 이상현상 서비스, 일차 런타임과 씬 연결 상태를 분석했다.
- 기획상 1일차의 튜토리얼, 정상화, 미발견, 현장 정전, 일차 종료 흐름에 필요한 구현 공백을 분류했다.

### 개발자 검토 및 결정

- 1차 목표는 테스트용 무작위 루프가 아니라 `기준 상태 확인 → D1_A01 튜토리얼 → 보고·성공 노이즈·복원 → 추가 이상현상 → 미발견/실패 → 정전 현장 사건 → 평가`가 끝까지 이어지는 일차 루프다.
- 이후 구현은 고정 튜토리얼·정상화 연출·일차 흐름 제어를 먼저 완성한 뒤 현장 이벤트와 콘텐츠를 확장한다.

### 구현 및 테스트 결과

- 코드 변경 없음. 현재 `TestDayDefinition`은 3개 랜덤 이상현상만 10~20초 간격으로 생성하는 테스트 구성임을 확인했다.

---

## 2026-07-22 — 1일차 시나리오 진행 제어 구조 구현

### 작업 목표

- 1일차의 브리핑, 기준 상태 확인, 첫 튜토리얼 이상현상, 일반 감시 시작을 분리된 진행 상태로 구현한다.

### AI 활용 방식

- 기존 `DayDefinition`, `DayRuntimeController`, `AnomalyScheduler`, `AnomalyService`의 책임을 분석해 데이터·진행·이벤트 통신을 분리하는 구조를 설계했다.
- 컴파일 검증을 통해 새 진행 제어 코드와 기존 시스템의 연결 오류를 확인했다.

### 개발자 검토 및 결정

- 1일차 튜토리얼 조건은 데이터 자산에 저장한다: 첫 이상현상 `D1_A01`, 채널 전환 3회 또는 감시 시작 후 60초.
- 튜토리얼 이상현상이 정상화되거나 미발견 처리될 때까지 랜덤 스케줄러를 일시정지한다.
- UI가 구현되기 전에는 Enter 키를 임시 진행 입력으로 사용하며, 이후 브리핑 UI가 `BeginBaselineReview`, `BeginMonitoring`을 호출하도록 확장한다.

### 구현 및 테스트 결과

- `Day1FlowController` 추가 및 `CCTVTest` 씬 연결 완료.
- 채널 선택·이상현상 해결 이벤트를 추가해 진행 상태 전환에 사용한다.
- `dotnet build Assembly-CSharp.csproj --no-restore` 성공: 오류 0개. 기존 API 사용 관련 경고만 존재.

### 후속 씬 분리

- 1일차 진행 구조를 `Assets/Scenes/Day1.unity`로 분리했다.
- `CCTVTest`는 진행 제어 컴포넌트 없이 자동 시작하는 플레인 테스트 씬으로 복구했다.
- 빌드 설정의 기본 활성 씬은 `Day1`로 변경했다.

### 후속 P0 — 정확 보고 정상화 연출

- 정확 보고의 상태를 즉시 해결하지 않고 `Normalizing`으로 전환한 뒤, 성공 노이즈 중간에 기준 상태를 복원하도록 변경했다.
- 정상화 중에는 보고 패널과 CCTV 패닝 입력을 잠그고, 연출이 끝난 뒤 다시 해제한다.
- 성공 전용 노이즈 프로필을 생성해 `Day1` 씬에 연결했다. 오보고는 기존 오보고 노이즈만 사용한다.
- `dotnet build Assembly-CSharp.csproj --no-restore` 성공: 오류 0개.

---

## 2026-07-22 — Day 1 정전 긴급 출동 상태 흐름 구현

### 작업 목표

- Day 1의 일반 감시 중 정전 조건을 감지하고, 긴급 출동과 복구 후 감시 복귀까지 게임 시간이 일관되게 제어되도록 구현.

### AI 활용 방식

- 기존 `DayRuntimeController`, `AnomalyScheduler`, `AnomalyService`와 CCTV/보고 입력 경로를 함께 분석해 긴급 상황에서 멈춰야 하는 시스템을 식별.
- 현장 맵과 플레이어 프리팹이 아직 씬에 없음을 확인한 뒤, 추후 현장 상호작용이 호출할 공개 복구 메서드와 임시 키보드 검증 입력을 분리해 설계.

### 개발자 검토 및 결정

- 발생 조건은 기획에 맞춰 `정답 보고 3회`와 `근무 진행도 70%`를 모두 충족하는 것으로 데이터화.
- 긴급 출동 중에는 근무 시간, 이상현상 타이머/생성, CCTV 채널 조작, 보고서 열기를 모두 중지해야 공정한 상태가 유지됨.
- 실제 배전반 상호작용은 이후 `Day1FlowController.CompleteEmergencyObjective()`를 호출하도록 연결하며, 현재는 테스트용 `E` 키로 같은 복구 경로를 검증.

### 구현 및 테스트 결과

- `DayDefinition`에 긴급 출동 활성화와 조건 데이터를 추가하고 Day 1 에셋에 적용.
- `Day1FlowController`에 `EmergencyDispatch`, `EmergencyRecovery` 상태 및 일시정지/복귀 처리를 추가.
- CCTV와 보고서 입력을 개별적으로 잠글 수 있도록 입력 제어 메서드를 추가.
- `dotnet build Assembly-CSharp.csproj --no-restore` 성공: 오류 0개.

### 협업 인수인계

- 시스템·콘텐츠와 UI·이펙트 역할을 분리하고, 현재 Day 1 루프·UI 연결점·후속 작업 우선순위를 `Day1_시스템_인수인계.md`로 문서화.
- AI는 실제 호출 가능한 공개 메서드와 이벤트를 기준으로 작업 목록을 정리했으며, 담당자는 상태값을 직접 변경하지 않고 해당 연결점을 사용한다.

### AI 활용 요소 제안 문서화

- 공모전 심사에 활용할 AI 기능을 제작 가속, 플레이 경험 강화, 활용 증명의 세 갈래로 재정리했다.
- 현재 구현 가능한 P0와 향후 확장 아이디어, 데모 구성, 데이터·저작권 안전장치를 `AI_활용_요소_제안서.md`에 기록했다.

---

## 2026-07-24 — Day 1 상태 UI 연결 컴포넌트

### 작업 목표

- UI 레이아웃과 시스템 로직을 분리한 채, Day 1 진행 상태가 화면 텍스트와 정전 오버레이에 자동 반영되게 연결.

### AI 활용 방식

- 기존 `Day1FlowController`의 상태·문구 이벤트를 분석해 UI가 직접 상태를 변경하지 않고 공개 메서드만 호출하도록 바인딩 구조를 설계.

### 구현 및 테스트 결과

- `Day1FlowStatusUI`를 추가했다. 상태 배지, 목표 문구, 정전 오버레이, 단일 행동 버튼을 Inspector에서 원하는 UI 오브젝트에 연결할 수 있다.
- 버튼은 상태에 따라 브리핑 진행, 감시 시작, 전력 복구를 호출한다. 씬 배치와 그래픽은 UI 담당 범위로 유지했다.
- 정답 보고, 오보고, 미보고 이벤트를 공용 피드백 텍스트 한 곳에 연결해 결과가 짧게 표시되도록 확장했다.
- UI 초기화 시 싱글턴 인스턴스를 새로 만들지 않도록, 피드백 이벤트 구독은 씬에 이미 배치된 `GameManager`만 찾도록 보완했다.

---

## 2026-07-24 — 정전 현장 카메라 전환 기반 구축

### 작업 목표

- 정전 시 Day1의 CCTV 감시 모드에서 CharacterMoveTest 기반 현장 횡스크롤 모드로 진입할 수 있는 기반을 마련.

### AI 활용 방식

- CharacterMoveTest의 실제 씬 구조와 필드 이동·카메라·입력 컴포넌트를 분석해, 중복 카메라와 매니저를 피하는 씬 이식 방식을 설계.

### 구현 및 테스트 결과

- `FieldModeController`를 추가해 CCTV UI/시스템을 비활성화하고 FieldModeRoot와 필드 플레이어·카메라를 활성화하는 진입 흐름을 구현했다.
- `Tools/Unrecorded Area/Import CharacterMove Field Into Day1` 메뉴를 추가했다. 수정이 끝난 CharacterMoveTest의 배경, 필드 카메라, 플레이어, 전역 조명만 Day1에 이식하며 원본 씬은 저장하지 않는다.
- 배전반 복구와 CCTV 복귀는 다음 단계로 의도적으로 제외했다.
- 일반 이상현상 대기 중 미보고 패배가 현장 카메라 검증을 방해하지 않도록, 튜토리얼 종료 뒤 F7으로 정전을 강제하는 에디터·개발 빌드 전용 테스트 진입점을 추가했다.
- 현장 전환 시 독립적으로 생성되는 `Canvas_CCTV`와 Day1에 기존 배치된 전역광이 남아 필드 화면을 덮거나 과도하게 밝히는 원인을 확인했다. 전환 시 해당 CCTV 캔버스와 필드 외 전역광을 끄도록 보완했다.
- 현장 카메라가 활성화 직후 플레이어 추적 보간으로 위로 미끄러지는 원인을 분석했다. 전환 첫 프레임에는 목표 위치로 즉시 맞추고, 그 뒤부터 기존의 부드러운 카메라 추적을 적용하도록 보완했다.
- 배전반 오브젝트에 재사용 가능한 `FieldBreakerPanel`을 연결해, 플레이어가 범위 안에서 `E`를 1.5초 유지하면 Day1 전력 복구 완료를 호출하도록 구현했다. 유지 시간은 Inspector에서 1~2초 범위로 조정할 수 있다.
- 보고서 선택지를 즉시 연속으로 갱신할 때 `Destroy`가 프레임 끝에 실행되어 이전 구역의 항목이 새 목록에 잠시 섞이는 문제를 분석했다. 기존 항목을 삭제 요청 전 즉시 비활성화하도록 보완했다.
- PRD의 ‘현장 이동 → 제어실 복귀 → 감시 재개’ 흐름을 재검증해, 배전반 복구 직후 감시 타이머를 재개하던 임시 로직을 수정했다. 복귀 문에서 제어실로 돌아온 뒤에만 CCTV 화면·조명·입력을 복원하고 감시 루프를 재개하도록 분리했다.

---

## 2026-07-25 — UI·이펙트 인수인계 문서화

### 작업 목표

- Day 1 시스템 구현 상태를 UI·이펙트 담당자가 즉시 연결·검증할 수 있는 작업 명세로 정리.

### AI 활용 방식

- 현재 코드의 상태 전환, Inspector 연결 필드, 현장 상호작용 컴포넌트를 대조해 역할 경계와 테스트 절차를 문서화.

### 구현 및 테스트 결과

- `Day1_UI_이펙트_인수인계_2026-07-25.md`에 정전·배전반·제어실 복귀·감시 재개 흐름, UI 연결점, 권장 연출 타임라인, 빠른 테스트 절차를 정리했다.

---

## 2026-07-26 — 미보고 임박 경고 오버레이 연결

### 작업 목표

- 이상현상 미보고 확정 5초 전부터 화면 최상단 비네트가 나타나며 긴급감을 전달하도록 구성.

### AI 활용 방식

- 기존 이상현상 타이머·해결·미보고 이벤트 흐름을 분석해, UI가 타이머를 직접 감시하지 않는 전용 긴급 경고 이벤트를 설계.
- 사용자가 만든 `GlobalEffectCanvas`를 CCTV CRT 효과와 분리된 공용 화면 오버레이로 유지하는 배치 방식을 제안.

### 개발자 검토 및 결정

- 경고 시작은 이상현상이 실제 활성 상태가 된 뒤 남은 시간이 5초 이하가 되는 단 한 번의 시점으로 제한.
- 정답 보고 시에는 비네트를 자연스럽게 숨기고, 미보고 확정 시에는 짧게 강하게 남긴 뒤 사라지게 결정.

### 구현 및 테스트 결과

- `AnomalyService`에 `AnomalyUrgencyStarted` 이벤트와 Inspector 조절값 `Missed Warning Lead Time Sec`를 추가.
- `MissedAnomalyWarningOverlay`를 추가해 CanvasGroup 알파 페이드와 자식 비네트 레이어의 확대·축소 펄스를 구현.
- 이벤트 수신용 `GlobalEffectCanvas`는 활성 상태로 유지하고, 실제 비네트 묶음만 평소에 비활성화했다가 경고 시 자동 활성화·종료 시 재비활성화하도록 보완.
- Unity Inspector에서 `GlobalEffectCanvas`에 컴포넌트를 붙여 실제 씬 연결 및 재생 검증이 필요.

---

## 2026-07-26 — 메인룸 허브 기반 Day 1 전환 구조

### 작업 목표

- `Area_MainRoom`을 전화·CCTV·외부 문으로 이어지는 Day 1의 중앙 허브로 재구성.

### AI 활용 방식

- 기존 정전 시점의 CCTV/현장 직접 전환과 복귀 로직을 추적해, 메인룸을 반드시 경유하는 상태 전환으로 재설계.
- 공간 활성화 책임과 게임 진행 책임을 분리하는 전환 컨트롤러·상호작용 컴포넌트 구조를 제안하고 구현.

### 구현 및 테스트 결과

- `Day1AreaTransitionController`에 메인룸, CCTV, 외부 필드의 활성화와 카메라 전환을 분리 구현.
- 기존 CCTV 구역과 동일하게 `Resources/Prefabs/AreaPrefabs/Area_MainRoom` 프리팹을 런타임 인스턴스로 생성하도록 보완. Inspector에 프리팹 에셋을 넣어도 자동 복제한다.
- 메인룸이 보이지 않던 원인을 `MainCamera`와 배경 스프라이트가 동일한 Z 좌표에 있어 Near Clip Plane 안쪽에서 잘리던 것으로 확인하고, 메인 카메라 Z 좌표를 `-10`으로 조정.
- CCTV 진입 직후 카메라가 다시 꺼질 수 있던 초기화 프레임 충돌을 제거하고, 늦게 생성되는 `Canvas_CCTV`만 메인룸 상태에서 비활성화하도록 수정.
- CCTV가 RenderTexture만 사용해 Unity Game 뷰에 `No cameras rendering`이 보이던 개발 편의 문제를 해결하기 위해, CCTV 중에는 `MainCamera`를 빈 검은 배경 표시용으로 유지하도록 보완.
- 메인룸 CCTV 클릭 시 카메라가 CCTV 오브젝트 중심으로 줌인하고, 전환 패널이 완전 불투명해진 뒤 CCTV 화면을 드러내는 연출 컴포넌트를 추가.
- 전환 패널의 `CanvasGroup`이 아닌 `Image`/`RawImage` Graphic 자체의 Color Alpha를 애니메이션하도록 변경.
- CCTV 중 `MainSceneUI`가 Canvas 전체를 숨겨 전환 패널의 페이드아웃까지 사라질 수 있던 문제를 해결: 안내 텍스트 묶음만 숨기고 전환 Canvas는 최상위 정렬 순서로 유지.
- 정전 시 `MainSceneUI`의 검은 Image를 두 번 점멸한 뒤 완전 암전하고, 확대된 메인룸 시야를 원래 카메라 시야로 되돌리며 메인룸을 공개하는 전환을 추가.
- 메인룸→CCTV와 정전→메인룸 연출을 `TransitionEffect` 하나로 통합하고, 전용 Transition Canvas 제어는 제거해 메인 Canvas의 Image를 직접 사용하도록 정리.
- 정전 최종 암전 뒤 검은 화면을 유지하는 시간을 `TransitionEffect` 조절값으로 추가.
- 점멸 종료 후 투명 상태와 최종 암전 사이의 간격을 별도 조절값으로 추가.
- 메인룸 문을 통한 외부 필드 진입에도 같은 검은 Panel 페이드인/아웃 전환을 연결.

---

## 2026-07-26 — 보고판 기본 선택값 동기화

### 작업 목표

- 보고판에 처음 표시되는 장소·물건·현상 문구와 실제 보고 판정 값을 일치.

### 구현 및 테스트 결과

- 각 목록을 만들 때 선택지 목록을 보존하고, 최초 또는 기존 선택이 무효한 경우에만 인덱스 0을 실제 선택값과 표시 텍스트로 확정.
- 이후 목록을 다시 열 때는 유효한 직전 선택을 유지해 클릭하지 않아도 화면 문구와 보고값이 일치.
- 후보가 달라지거나 이전 선택이 목록에서 사라져도 자동으로 0번 항목으로 되돌리지 않고, 사용자가 마지막으로 확정한 보고값을 유지하도록 보정.
- 전화·CCTV·문에 붙일 `MainRoomInteractionTarget` 및 상태별 사용 가능 여부를 제어하는 `MainRoomInteractionController`를 추가.
- 정전 시 메인룸으로 복귀, 외부 문 클릭으로 현장 진입, 현장 복구 뒤 메인룸 복귀, CCTV 클릭으로 감시 재개 흐름으로 `Day1FlowController`를 변경.
- 프리팹에 실제 상호작용 오브젝트와 Collider2D를 배치한 뒤 Inspector 연결 및 Unity 플레이 검증이 필요.

---

## 2026-07-26 — 메인 장면 상황·행동 안내 UI

### 작업 목표

- 현재 상황과 플레이어의 다음 행동을 두 개의 독립 텍스트로 지속 안내.

### AI 활용 방식

- Day 1 진행 상태와 공간 전환 상태를 대조해, 같은 정전 상태라도 메인룸/외부 현장에서 서로 다른 행동 문구가 나오도록 상태표를 설계.

### 구현 및 테스트 결과

- `MainSceneUI`를 추가해 `Day1FlowController`와 `Day1AreaTransitionController` 이벤트에 따라 상황·행동 텍스트를 자동 갱신.
- CCTV 관람 중에는 CanvasGroup 알파를 0으로 전환해 메인 장면 안내를 숨기고, 메인룸·외부 현장 복귀 시 자동 재표시.
- 미보고 임박 비네트는 CCTV 전용 효과로 제한해, 메인룸·외부 필드 전환 시 즉시 제거되도록 공간 전환 이벤트와 연결.
- Canvas와 TMP 텍스트 배치는 사용자가 만들고, 컴포넌트에 두 텍스트만 연결하면 된다.

---

## YYYY-MM-DD — 작업 제목

### 작업 목표

- (작성 예정)

### AI 활용 방식

- AI에게 요청한 핵심 내용:
- AI가 제공한 결과:
- 사용 도구 또는 방식:

### 개발자 검토 및 결정

- 검토한 내용:
- 채택/수정/미채택 결정 및 이유:

### 구현 및 테스트 결과

- 변경 파일 또는 기능:
- 테스트 방법:
- 결과 및 남은 과제:
# 2026-07-27

## 이상현상 발생 은폐 연출 설계 및 연결

- AI와 함께 기존 이상현상 적용 흐름을 점검해, `AnomalyService`가 오브젝트 상태를 즉시 변경해 이동 장면이 노출될 수 있음을 확인했다.
- `AnomalyAppearanceBlinkOverlay`를 추가했다. 위·아래 UI 패널이 빠르게 두 번 깜빡인 뒤, 천천히 닫히고, 상태 변경 후 즉시 열리는 눈꺼풀 연출을 담당한다.
- 이상현상 데이터의 경고 시간 이후, 화면이 완전히 닫힌 시점에만 `AnomalyService`가 실제 액션을 적용하도록 연결했다.
- 기존 미보고 경고(붉은 비네트)와는 독립된 연출이다. CCTV 화면이 아닐 때는 연출과 대기 없이 즉시 이상현상을 적용한다.
- 빠른 눈깜빡임 사이 간격을 인스펙터에서 조절할 수 있도록 추가했다.

## 미보고 신호 유실 연출 구현

- 기존 CRT 셰이더와 노이즈 프로필 구조를 분석해, 오답 보고 노이즈를 재사용하던 미보고 연출을 전용 신호 유실 연출로 분리했다.
- CCTV 피드를 0.1초 고정한 뒤, 강한 노이즈·스캔라인·색수차·수평 찢김을 포함한 0.48초 프로필을 재생한다.
- 재생 중 CCTV 시간 표시를 무작위 숫자로 깨뜨리고, 종료 후 실제 게임 시간으로 복구한다.
