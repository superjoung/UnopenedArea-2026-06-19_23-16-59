# Day 1 시스템 인수인계

작성일: 2026-07-22
역할: 시스템·콘텐츠 개발 — Codex / UI·이펙트 개발 — 동료

## 현재 플레이 루프

1. `Day1` 씬을 실행한다.
2. `Enter`로 브리핑과 기준 상태 확인을 지나 감시를 시작한다. 이는 브리핑 UI가 붙기 전 임시 입력이다.
3. CCTV 채널을 3회 전환하거나 감시 시작 후 60초가 지나면 튜토리얼 이상현상 `D1_A01`이 나타난다.
4. 플레이어가 보고서를 열어 정답을 제출하면 성공 노이즈가 재생되고, 노이즈 중간에 CCTV 화면이 기준 상태로 복원된다. 보고/패닝 입력은 연출 중 잠긴다.
5. 튜토리얼이 해결되거나 미보고로 끝나면 일반 랜덤 감시가 시작된다. 현재 `TestDayDefinition`은 10~20초마다 D1_A01~A03 중 하나를 발생시킨다.
6. 이상현상은 발생 뒤 20초 안에 정답 보고를 해야 한다. 시간을 넘기면 기준 상태로 복원되고 실패 횟수가 1 증가하며 미보고 노이즈가 재생된다. 실패 3회면 Day 실패다.
7. 정답 보고가 3회이고 근무 시간이 70%(600초 중 420초)를 넘으면 정전 긴급 출동 상태가 한 번 발생한다. 이때 근무 시간, 이상현상, CCTV/보고 입력이 모두 멈춘다. 현재는 `E` 키로 테스트 복구하며, 복구 뒤 감시를 재개한다.

## 시스템 개발자가 만든 연결점

- `Day1FlowController`: Day 1 진행 상태와 튜토리얼·정전 흐름의 중심. 현장 상호작용은 `CompleteEmergencyObjective()`를 호출하면 복구된다.
- `DayDefinition`: 튜토리얼과 정전 조건을 데이터로 보관한다.
- `AnomalyService`: 정답 보고를 즉시 제거하지 않고 `Normalizing` 상태로 유지해 성공 연출 중간에 화면을 복원한다.
- `CCTVTestSceneController`, `GameManager`: 긴급 출동 중 CCTV 채널 및 보고서 입력을 각각 잠글 수 있다.
- 기본 빌드 씬은 `Day1`, `CCTVTest`는 플레인 테스트 씬이다.

## UI·이펙트 담당 작업 목록

### P0 — 플레이에 반드시 보여야 하는 UI

- Day 1 진행 안내 패널: `Briefing`, `BaselineReview`, `Monitoring` 상태 메시지를 보여주고, Enter 임시 입력 대신 `다음`/`감시 시작` 버튼으로 `BeginBaselineReview()`와 `BeginMonitoring()`을 호출.
- 정전 긴급 출동 오버레이: 정전 발생 시 화면을 어둡게 전환하고 `정전 발생 / 현장 복구 필요`를 안내. 복구 중에는 CCTV/보고 UI가 열린 상태로 남지 않도록 닫거나 숨김.
- 정전 테스트용 복구 버튼: 우선 버튼의 OnClick에 `Day1FlowController.CompleteEmergencyObjective()` 연결. 실제 현장 맵이 완성되면 배전반 상호작용으로 대체.
- Day 상태/실패 피드백: 미보고 시 실패 수가 증가했음을 즉시 읽을 수 있게 카운터·짧은 문구·색 변화로 표시.

### P1 — CCTV 연출 품질

- 이상현상 발생 전환 노이즈: 현재는 이상현상이 즉시 나타난다. `랜덤 대기 종료 → 0.3~0.7초 노이즈 → 이상현상 적용` 연출을 원한다면 시스템 담당과 연동해 추가.
- 성공 보고 노이즈: 이미 `CCTVSuccessReportNoiseProfile`이 연결돼 있다. 강도와 길이를 조정하고, 기준 상태 복원이 노이즈 중간에 자연스럽게 가려지는지 확인.
- 미보고/오보고 노이즈: 현재 동일한 실패 프로필을 사용한다. 미보고은 짧은 경고, 오보고는 더 강한 오류처럼 분리할지 기획 결정 후 프로필을 분리.
- 정전 연출: CRT 붕괴, 화면 블랙아웃, 전력 복구 플래시를 제작. 정전 시작과 복구 완료에 맞춰 시스템 이벤트를 구독하는 방식으로 연결.

### P2 — 현장 출동 화면

- 별도 현장 맵/플레이어 프리팹 구성.
- 배전반 상호작용 UI와 거리/상호작용 안내 제작.
- 배전반 복구 성공 시 `CompleteEmergencyObjective()`를 호출.
- CCTV 화면과 현장 화면 전환, 복귀 시 CCTV UI 상태 복원 연출 제작.

## 협업 시 주의 사항

- `Day1FlowController.FlowMessageChanged`와 `StateChanged`는 UI 바인딩용 이벤트다.
- UI가 상태를 바꾸고 싶을 때는 상태값을 직접 수정하지 말고 공개 메서드를 호출한다.
- 정전 조건은 `TestDayDefinition`의 `emergencyRequiredCorrectReports`, `emergencyTriggerProgress`로 조절한다. 빠른 테스트 시 각각 `1`, `0.1`을 권장하며, 제출용 기본값은 `3`, `0.7`이다.
- 로그의 `미보고 노이즈 실행`은 새 이상현상 생성이 아니라, 발생한 이상현상을 20초 내 보고하지 못했음을 뜻한다.

## 검증 상태

- `dotnet build Assembly-CSharp.csproj --no-restore` 성공, 오류 0개.
- Unity 에디터에서의 실제 플레이 검증과 정전 시각 연출은 UI·이펙트 작업 이후 함께 확인한다.
